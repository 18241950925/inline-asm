void hpu_ntt_N65536(void) {
    __asm__ volatile(
        // Load input poly for standalone NTT test
        "dload x0, x0, p0, 1 \n\t"
        // Load mod context for standalone NTT test
        "dload x0, x0, p4, 2 \n\t"
        "pmodld p4, 0, 0 \n\t"
        // Active mod context must be loaded before pntt stages

        // ==========================================
        // Stage 0 (Stage-level pntt)
        // ==========================================
        "dload x0, x0, p3, 1 \n\t"
        "pntt p0, p3, 0, 0, 0 \n\t"

        // ==========================================
        // Stage 1 (Stage-level pntt)
        // ==========================================
        "dload x0, x0, p3, 1 \n\t"
        "pntt p0, p3, 1, 0, 0 \n\t"

        // ==========================================
        // Stage 2 (Stage-level pntt)
        // ==========================================
        "dload x0, x0, p3, 1 \n\t"
        "pntt p0, p3, 2, 0, 0 \n\t"

        // ==========================================
        // Stage 3 (Stage-level pntt)
        // ==========================================
        "dload x0, x0, p3, 1 \n\t"
        "pntt p0, p3, 3, 0, 0 \n\t"

        // ==========================================
        // Stage 4 (Stage-level pntt)
        // ==========================================
        "dload x0, x0, p3, 1 \n\t"
        "pntt p0, p3, 4, 0, 0 \n\t"

        // ==========================================
        // Stage 5 (Stage-level pntt)
        // ==========================================
        "dload x0, x0, p3, 1 \n\t"
        "pntt p0, p3, 5, 0, 0 \n\t"

        // ==========================================
        // Stage 6 (Stage-level pntt)
        // ==========================================
        "dload x0, x0, p3, 1 \n\t"
        "pntt p0, p3, 6, 0, 0 \n\t"

        // ==========================================
        // Stage 7 (Stage-level pntt)
        // ==========================================
        "dload x0, x0, p3, 1 \n\t"
        "pntt p0, p3, 7, 0, 0 \n\t"

        // ==========================================
        // Stage 8 (Stage-level pntt)
        // ==========================================
        "dload x0, x0, p3, 1 \n\t"
        "pntt p0, p3, 8, 0, 0 \n\t"

        // ==========================================
        // Stage 9 (Stage-level pntt)
        // ==========================================
        "dload x0, x0, p3, 1 \n\t"
        "pntt p0, p3, 9, 0, 0 \n\t"

        // ==========================================
        // Stage 10 (Stage-level pntt)
        // ==========================================
        "dload x0, x0, p3, 1 \n\t"
        "pntt p0, p3, 10, 0, 0 \n\t"

        // ==========================================
        // Stage 11 (Stage-level pntt)
        // ==========================================
        "dload x0, x0, p3, 1 \n\t"
        "pntt p0, p3, 11, 0, 0 \n\t"

        // ==========================================
        // Stage 12 (Stage-level pntt)
        // ==========================================
        "dload x0, x0, p3, 1 \n\t"
        "pntt p0, p3, 12, 0, 0 \n\t"

        // ==========================================
        // Stage 13 (Stage-level pntt)
        // ==========================================
        "dload x0, x0, p3, 1 \n\t"
        "pntt p0, p3, 13, 0, 0 \n\t"

        // ==========================================
        // Stage 14 (Stage-level pntt)
        // ==========================================
        "dload x0, x0, p3, 1 \n\t"
        "pntt p0, p3, 14, 0, 0 \n\t"

        // ==========================================
        // Stage 15 (Stage-level pntt)
        // ==========================================
        "dload x0, x0, p3, 1 \n\t"
        "pntt p0, p3, 15, 0, 0 \n\t"
        "psync 0, 0 \n\t"

        // Final result object slot: p0

        // 结束
        :
        :
        : "memory"
    );
}
